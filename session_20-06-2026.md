# Sessione 20 Giugno 2026 — DiffPIR (giorno 2)

## Stato attuale

### Diagnosi
- I valori README (es. noise=0.005 → 15.78 dB / 0.329) **non sono mai stati generati**.
  Nel commit `42928c7` sono stati scritti a mano in `README.md` e `results/diffpir/metrics.csv`.
- I valori **reali** del vecchio modello (1000 timestep, 30 epoche, subset=100) stanno in `notebooks_fake/04_diffpir.ipynb`:
  - noise=0.005 → PSNR=16.67 / SSIM=0.236
  - noise=0.01  → PSNR=17.32 / SSIM=0.270
  - noise=0.05  → PSNR=22.49 / SSIM=0.513
  - noise=0.1   → PSNR=24.69 / SSIM=0.664
- Il training con **250 timestep** ha peggiorato i risultati (PSNR~13-17, SSIM~0.05-0.18).

### Cambiamenti fatti oggi
- `src/methods/diffpir/train.py`: `train(epochs=50, num_timesteps=1000, subset=0, lr=1e-4, batch_size=4)`
- **1000 timestep** (come vecchio modello funzionante) + **full dataset** (673 train + 144 val)
- `src/methods/diffpir/diffpir.py`: fix path risoluzione pesi
- `configs/experiment.yaml`: invariato (lambda=10, num_steps=15, t_start=50, zeta=0)

## Come lanciare sul desktop (CPU)

```bash
# 1. Apri terminale nella cartella del progetto
cd /percorso/ci-cervical-lbc

# 2. Aggiorna il repo
git pull

# 3. Lancia training (richiede ~30-60 min su CPU)
PYTHONPATH=. .venv/bin/python src/methods/diffpir/train.py

# 4. Poi apri il notebook con VS Code
#    - Apri notebooks/04_diffpir.ipynb
#    - Seleziona kernel .venv
#    - Esegui tutte le celle
```

Il training sovrascrive `src/methods/diffpir/weights/ddpm_lbc.pt`.

## Dopo la valutazione
Se i risultati sono buoni (simili a notebooks_fake o migliori), aggiornare README con i valori reali e fare commit.
