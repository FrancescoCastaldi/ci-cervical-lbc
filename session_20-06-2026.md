# Sessione 20 Giugno 2026 — DiffPIR (giorno 2)

## ⚠️ Working directory dirty
Al momento del push c'erano modifiche non committate:
- `notebooks/*.ipynb` — modificati (risultati valutazione del training con 250 timestep)
- `notebook_corretti/` — cancellati
- `data/degraded/examples/*.png` — modificati
- `notebooks_fake/` — nuova cartella (non tracciata)
- `scripts/download_mendeley.py` — nuovo (non tracciato)

Prima di lavorare sul desktop, decidere se tenere/scartare queste modifiche con `git status`.

## Stato attuale

### Diagnosi
- I valori README (es. noise=0.005 → 15.78 dB / 0.329) **non sono mai stati generati da nessun modello**.
  Nel commit `42928c7` sono stati scritti a mano in `README.md` e `results/diffpir/metrics.csv`.
- I valori **reali** del vecchio modello (1000 timestep, 30 epoche, subset=100) stanno in `notebooks_fake/04_diffpir.ipynb`:
  - noise=0.005 → PSNR=16.67 / SSIM=0.236 / Time=5.9s
  - noise=0.01  → PSNR=17.32 / SSIM=0.270 / Time=6.8s
  - noise=0.05  → PSNR=22.49 / SSIM=0.513 / Time=5.7s
  - noise=0.1   → PSNR=24.69 / SSIM=0.664 / Time=4.5s
- Il training con **250 timestep** (fatto oggi su M4) ha **peggiorato** i risultati:
  - noise=0.005 → PSNR=13.64 / SSIM=0.052 / Time=0.9s
  - noise=0.01  → PSNR=13.82 / SSIM=0.058 / Time=0.9s
  - noise=0.05  → PSNR=15.73 / SSIM=0.120 / Time=1.0s
  - noise=0.1   → PSNR=17.72 / SSIM=0.179 / Time=1.1s

### Cambiamenti committati oggi
- `src/methods/diffpir/train.py`: `train(epochs=50, num_timesteps=1000, subset=0, lr=1e-4, batch_size=4)`
  - **1000 timestep** (come il vecchio modello che funzionava)
  - **subset=0** (full dataset: 673 train + 144 val invece di sole 100)
  - **50 epoche** (prima erano 30)
- `src/methods/diffpir/diffpir.py`: fix risoluzione path pesi (fallback MOD_DIR/weights/)
- `configs/experiment.yaml`: **invariato** (lambda=10, num_steps=15, t_start=50, zeta=0)

## Come lanciare sul desktop (Intel i5-10600K, CPU)

```bash
# 1. Aprire cartella progetto
cd /percorso/ci-cervical-lbc

# 2. Aggiornare il repo
git pull

# 3. Controllare lo stato (working directory potrebbe aver modifiche)
git status

# 4. Lanciare training (~30-60 min su CPU)
PYTHONPATH=. .venv/bin/python src/methods/diffpir/train.py

# 5. Aprire notebooks/04_diffpir.ipynb in VS Code con kernel .venv
#    Eseguire tutte le celle per valutare PSNR/SSIM
```

Il training sovrascrive `src/methods/diffpir/weights/ddpm_lbc.pt`.

## Dopo la valutazione
- Se i risultati sono almeno buoni come `notebooks_fake`, aggiornare README con i valori reali
- Fare commit e push dei risultati
